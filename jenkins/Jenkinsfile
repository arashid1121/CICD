pipeline {
       agent any
       stages {
        stage('Build') { 
            steps {
                echo 'Hello All'
                echo env.WORKSPACE
                sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
}
