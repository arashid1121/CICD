pipeline {
       agent any
       stages {
        stage('Build') { 
            steps {
                echo pwd
                cd 'CICD'
                echo pwd
                sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
}
